% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/processar_caged.R
\name{processar_caged}
\alias{processar_caged}
\title{Motor Analítico de Processamento do Novo CAGED}
\usage{
processar_caged(
  usar_temporario = FALSE,
  parquet_individual = FALSE,
  origem = "dados_caged_raw",
  destino = "dados_caged_parquet"
)
}
\arguments{
\item{usar_temporario}{Lógico. Define a origem dos dados. Se \code{TRUE}, a função processará
arquivos recém-baixados na memória volátil (\code{tempdir()}). Se \code{FALSE}, processará
arquivos em um diretório local permanente (útil para desenvolvimento ou auditoria).}

\item{parquet_individual}{Lógico. Controle de saída. Se \code{TRUE}, gera um arquivo \code{.parquet}
independente para cada aba do Excel (ideal para modelagem de BI corporativo).
Se \code{FALSE} (padrão), une todas as tabelas em um único dataset consolidado.}

\item{origem}{String. Caminho do diretório de onde o arquivo \code{.xlsx} será lido. Se \code{NULL}
e \code{usar_temporario} for \code{TRUE}, o pacote buscará na pasta temporária. Utilizado
internamente para testes com mock data (ex: miniatura do CAGED).}

\item{destino}{String. Caminho do diretório onde os arquivos \code{.parquet} finais
serão salvos. Se \code{NULL}, salva no diretório de trabalho atual ou pasta temporária.}
}
\value{
Retorna invisivelmente uma conexão/Dataset (\code{arrow::open_dataset()})
apontando para os arquivos processados, pronto para consultas \emph{Lazy Evaluation}.
Se nenhum dado for processado, retorna \code{NULL}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#maturing}{\figure{lifecycle-maturing.svg}{options: alt='[Maturing]'}}}{\strong{[Maturing]}}

A função \code{processar_caged()} atua como o motor interno de transformação do pacote.
Ela lê os arquivos originais \code{.xlsx} (com as 11 abas distintas), padroniza a estrutura
dos cabeçalhos, lida com formatações dinâmicas e consolida os dados em uma arquitetura
tabular otimizada. O resultado é persistido em disco utilizando o formato colunar Apache Parquet.
}
\examples{
\dontrun{
# Processamento de um arquivo local para geração de base consolidada
# base_pronta <- processar_caged(
#   usar_temporario = FALSE,
#   origem = "pasta_com_excel_caged/",
#   parquet_individual = FALSE
# )

# Gerando arquivos independentes para Power BI / Tableau
# processar_caged(parquet_individual = TRUE)
}
}
